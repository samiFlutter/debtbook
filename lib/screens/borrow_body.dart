import 'package:flutter/material.dart';

class BorrowContainer extends StatelessWidget {
  const BorrowContainer({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('Borrow container'),
    );
  }
}
